let numeros = [];

numeros[0] = 20;
numeros[1] = 25;
numeros[2] = 30;
numeros[3] = 35;
numeros[10] = "20";

for(let i = 0; i < numeros.length; i++){
    console.log(numeros[i]);
}

let numeros2 = [10, 8, 0, 24, 55];

for(let i = 0; i < numeros2.length; i++){
    console.log(numeros2[i]);
}